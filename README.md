# Waste Classification from Images using SVM

This project is a machine learning web application that classifies waste images into:

- Organic Waste
- Recyclable Waste

The system uses:
- HOG (Histogram of Oriented Gradients) for feature extraction
- SVM (Support Vector Machine) for classification
- Streamlit for the web interface

## How to Run
```bash
streamlit run app.py
